import { OneCharUpperPipe } from './one-char-upper.pipe';

describe('OneCharUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new OneCharUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
