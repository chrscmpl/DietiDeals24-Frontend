import { StretchOnScrollDirective } from './stretch-on-scroll.directive';

describe('StretchOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
